// Local configuration overrides (NOT committed to git)
// Copy this file to config.local.js for local development
const CONFIG = {
    SERVER_URL: 'http://localhost:3000'  // Use local server for development
};
